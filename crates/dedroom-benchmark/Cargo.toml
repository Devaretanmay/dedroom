[package]
name = "dedroom-benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
dedroom-core = { path = "../dedroom-core" }
tiktoken-rs = "0.6"
tokio = { version = "1", features = ["full"] }
serde_json = "1.0"
tempfile = "3.2"
